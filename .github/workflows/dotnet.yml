# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x
    - name: Restore dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build --no-restore

###### TRIVY CODE SCANNER START

    #- name: Aqua Security Trivy
    #  uses: aquasecurity/trivy-action@0.28.0
      
    #- name: Upload Trivy Scan Result
    #  uses: github/codeql-action/upload-sarif@v3
    #  with:
    #    sarif_file: trivy-results1.sarif
    - name: Aqua Build Security
  # You may pin to the exact commit or the version.
  # uses: aquasecurity/build-security-action@d888ad3abadb4bbca1fcff8d83c416c502e39b1a
      uses: aquasecurity/build-security-action@v0.0.1
      with:
        # Your AQUA_KEY provided by CSPM
        aqua_key: ''
        # Your AQUA_SECRET provided by CSPM
        aqua_secret: ''
        
###### TRIVY CODE SCANNER END
    
    - name: Test
      run: dotnet test --no-build --verbosity normal
